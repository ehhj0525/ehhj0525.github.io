# Inbox

Drop photos here — by drag-and-drop in the GitHub web UI, or from the upload page.

The pipeline empties this folder on every run: each photo becomes `web/<hash>.jpg`
and `thumbs/<hash>.jpg`, and its date and location are recorded in `photos.json`.
The original is then deleted, so **keep your own copies** — this repository is a
gallery, not a backup.
